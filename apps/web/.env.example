# Copy to .env.local and adjust for your environment.
# Point the browser/app at the registry API. When unset, requests go through
# the Next.js rewrite proxy (single-origin, recommended for previews).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
